Use the challenger agent on $ARGUMENTS.

If $ARGUMENTS is empty, ask the user: "Which file or function would you like to challenge yourself on?"

---

**After this session ends:** run `/simplify` to review any changes you made for redundancy, missed reuse, or efficiency issues.
